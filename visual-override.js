/*
 * Hirundo — override.css generator
 * Reserved hook for editor-driven CSS overrides. All brand theming is handled
 * in theme-variables.js + style.css, so nothing extra is emitted here.
 */
var generateOverride = function () {
    return '';
};

module.exports = generateOverride;
